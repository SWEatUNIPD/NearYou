describe('Simulator', () => {
    it('should remove rent ended', () => {
        // updateRentEnded deve rimuovere il rent (che esiste)
    });

    it('shoul throw rent ended index not found error', () => {
        // updateRentEnded deve lanciare l'eccezione se il rent non esiste
    });

    it('should receive rent ended update', () => {
        // se il rent fa il notify il simulator deve ricevere l'update (observer funzionante)
    });
});
